package main

import "log/slog"

func main() {

	slog.Info("starting server")

	slog.Info("Starting server")

	slog.Info("server started!!!")

	password := "secret"
	slog.Info("password: " + password)
}
